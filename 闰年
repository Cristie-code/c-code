#include<stdio.h>
int run(int a)
{
	if (a % 4 == 0)
	{
		return 1;
	}
	return 0;
}
int main(void)
{
	int year = 0;
	for (year = 1000; year <= 2000; year++)
	{
		
		if (run(year) == 1)
		{
			printf("%d", year);
		}
	}
	return 0;
}
